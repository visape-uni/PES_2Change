<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group>
        <item
            android:id="@+id/explore"
            android:name="@string/explore" />
        <item
            android:id="@+id/lists"
            android:name="@string/my_lists" />
        <item
            android:id="@+id/chat"
            android:name="@string/chat" />
        <item
            android:id="@+id/profile"
            android:name="@string/profile" />
    </group>

    <group>
        <item
            android:id="@+id/settings"
            android:name="@string/settings" />
        <item
            android:id="@+id/help"
            android:name="@string/help" />
        <item
            android:id="@+id/about"
            android:name="@string/about" />
    </group>

    <group>
        <item
            android:id="@+id/logout"
            android:name="@string/logout" />
    </group>

</menu>